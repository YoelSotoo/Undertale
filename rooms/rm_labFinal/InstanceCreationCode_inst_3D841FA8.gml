// Room de destino
target_rm = rm_carreratest;  // Cambia por la room que quieras
target_x = 0;           // X destino si quieres mover al player
target_y = 0;           // Y destino si quieres mover al player

// Tipo de TP
maintain_pos = true;      // true = mantener posición del player (para tienda u overlay)

// Bloquear movimiento del player al entrar
block_player = true;  

